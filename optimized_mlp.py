import torch
import torch.nn as nn

class SimpleMLPOptimized(nn.Module):
    def __init__(self):
        super().__init__()
        self.seq = Module()
        
    def forward(self, x):
        return self.model(x)
